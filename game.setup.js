//////////////////////////////////
// SET UP INITIAL VARIABLES
//////////////////////////////////

// Set maximum flips for each medal

var goldMax = 45;
var silverMax = 60;
var bronzeMax = 75;

// Set up the max flips, which will automatically
// Reduce with each level and selected skill level
// Change if the game is too easy or difficult

var setMaxFlips = '100'; // Max card flips (flipCounter)
var setMaxTime = '100'; // Max time (myTimer)